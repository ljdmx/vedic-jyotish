# Project TODO

- [x] 审阅 GitHub 源码、依赖、构建脚本与环境变量边界。
- [x] 迁移 `client/`、`server/` 与 `shared/` 中的应用源码并保持 tRPC 计算路由完整。
- [x] 迁移纸墨天文手稿视觉样式、字体配置及静态资源引用。
- [x] 保留高德地图选点、地址搜索与时区回填的服务端同源代理。
- [x] 保留由服务端 `AGNES_API_KEY` 调用的 AI 占星报告功能。
- [x] 以受管密钥配置 `AGNES_API_KEY`、`AMAP_WEB_SERVICE_KEY`、`AMAP_JS_API_KEY` 与 `AMAP_SECURITY_JS_CODE`，避免服务端密钥泄露。
- [x] 执行 TypeScript 检查、Vitest 测试与生产构建。
- [x] 验证首页及核心排盘工作流可访问，并保存可公开发布的版本。
- [x] 核查受管手稿图片与 favicon 的运行时引用，确认不存在遗留的项目内图片路径。
- [x] 核对 DeepSeek、Kimi、Qwen、GLM 与 AIAPI.world 的流式协议兼容性，并明确当前实现的可靠性边界。
- [x] 补充核对 Kimi、Qwen、GLM 的具体流式或 Chat Completion 官方规范，记录增量字段、结束标记与异常语义。
- [x] 核实 GLM 在模型配置中的 Chat Completion 兼容端点，并与用户选择的 GLM 凭据类型匹配。
- [x] 为各可选供应商补充最小流式兼容性测试，覆盖分块、空增量、结束事件与异常中断。
- [x] 诊断流式报告是否存在重复渲染、渲染卡顿或长时间停顿，并明确触发条件。
- [x] 核查 GitHub `client/public/images` 静态图片失效的原因、影响范围及适合仓库与部署端的修复方案。
- [x] 优化流式报告的事件去重、完成状态校验与异常断流反馈。
- [x] 将流式 Markdown 从稳定内容全量重建改为按已闭合块增量追加，降低长报告卡顿。
- [x] 改善流式等待提示，并补充重复事件、缺失完成事件和增量渲染的回归测试。
- [x] 为流式增量渲染补充 DOM 回归测试，验证稳定块追加、尾部更新及重启清空不会重建既有稳定内容。
- [x] 将 Prashna 时区初始值改为空并显示“定位后自动回填”，使其与出生信息排盘的定位逻辑一致。
- [x] 将当前代码更新及纸墨背景静态资源同步推送至关联 GitHub 仓库，并核验远程文件可访问。
- [x] 核对关联 GitHub 主分支中的当前代码文件与本地内容哈希一致。
- [x] 抽检 GitHub 背景图的原始下载链接返回成功且文件非零字节。
- [x] 核对关联 GitHub 仓库的 `client/public/images` 已包含全部九张纸墨背景资源。
- [x] 审阅仓库模板与迁移遗留文件的实际引用关系，区分运行必需项与可安全清理项。
- [x] 逐项核对 Drizzle、组件配置、模板元数据、补丁、测试与文档文件是否被构建、运行时或开发脚本引用。
- [x] 输出“运行必需、开发必需、可安全删除”三类仓库文件清单及其依据。
- [x] 将仓库文件用途、引用依据与清理风险写入可验证的审阅文档。
- [x] 清理确认无用的数据库模板、模板元数据与维护遗留文件，同时保留运行和构建必需项。
- [x] 验证精简后测试和生产构建通过，并同步删除项至关联 GitHub 仓库。
- [x] 将精简删除项同步至关联 GitHub 主分支，并核验远程模板遗留文件已不存在、核心目录与背景图仍保留。
- [x] 复核当前流式报告的去重、增量渲染与停顿反馈行为，并说明可保障范围与外部模型边界。
- [x] 增强流式报告的旧流隔离与可恢复的长停顿反馈。
- [x] 补充流式性能与中断恢复回归测试，并完整验证前端显示行为。
- [x] 完善 README 的产品介绍、流式保障、部署与仓库使用说明。
- [x] 为长尾 Markdown 渲染增加增量性能边界测试，并为中断、长等待与停止状态增加 DOM 回归测试。
- [x] 将更新后的 README、截图与流式优化同步到 GitHub，并保存自动部署版本。
- [x] 更新公开页面截图，完成生产构建、保存版本并自动部署。
- [x] 提取报告流状态展示文案并补充中断、长等待与停止生成的前端状态回归测试。
- [x] 为报告抽屉补充 DOM/UI 回归测试，覆盖中断、长等待、停止生成和部分报告保留提示。
- [x] 使用真实报告流完成页面级验证，覆盖生成中、停止生成以及中断后保留部分报告的显示状态。
- [x] 修复 GitHub README 中的截图显示，改用 GitHub 可长期访问的图片资源。
- [x] 补充本地运行、受管环境变量、manus.space 发布、域名与 GitHub 同步的线上部署教程。
- [x] 验证 README 图片和文档步骤，保存并自动部署修正版本。
- [x] 保存包含 README 截图修复与部署教程更新的新版 checkpoint，并确认自动部署成功。
- [x] 推送 README 修订至关联 GitHub 主分支，并核验远程页面实际渲染两张截图与部署教程。
- [x] 修复高德地图同源代理对 JSONP 日志请求返回错误 MIME 类型的问题。
- [x] 诊断并降低应用自身可控的定时器与强制重排开销，同时记录不可控的浏览器扩展与地图 SDK 告警边界。
- [x] 验证地图加载、控制台状态与生产构建，保存修复发布版本。
- [x] 审阅报告流协议、客户端缓冲调度、取消与完成切换，定位进一步可优化的边界。
- [x] 为高频增量引入可验证的背压控制与完成阶段一致性优化，避免尾部内容丢失或重复定型。
- [x] 扩展流式回归测试，验证高频分块、停止生成、错误中断和完成切换；完成构建与页面验证后自动发布。
- [x] 复现并定位流式报告在生成期间重复从头渲染的触发路径。
- [x] 保留已稳定的流式 DOM 内容，抑制非必要的 restart 清屏和完成阶段回退重渲染。
- [x] 为重复 restart、完成定型和长报告追加补充回归测试，验证后自动发布修复版本。
- [x] 审阅流式事件、输入质量、报告证据覆盖和不确定性边界，明确可提升且可验证的准确性代理指标。
- [x] 为流式过程增加轻量事件指标，并为报告补充计算依据、输入质量与结论边界的结构化约束。
- [x] 扩展流式与报告质量回归测试，完成构建和页面验证后自动发布改进版本。
- [x] 审阅流式传输的长等待反馈与出生日期、时区、坐标、时制的一致性校验边界。
- [x] 增加无内容 SSE 心跳事件与出生时空一致性质量提示，降低等待不确定性并避免输入误配。
- [x] 增加心跳解析、时空校验与报告边界回归测试，完成构建和页面验证后自动发布。
- [x] 审阅 Prashna 的现有白名单规则、可用盘面字段与报告中“边界说明”占比。
- [x] 实现基于当前可计算 Prashna 规则的明确裁决、支持/制约证据链与条件式时间窗口，压缩冗长免责声明。
- [x] 优化 Prashna 的流式分段与终态定型，补充裁决、证据和连续渲染回归测试，并完成完整构建验证。
- [x] 为 Prashna 报告新增专属流式回归测试：裁决摘要首段先输出、后续增量续写不清屏、终态保留原始 DOM/摘要不被重绘。
- [x] 保存包含 Prashna 裁决与流式改进的新版 checkpoint，并确认公开站点自动部署成功后记录版本号。
- [x] 审阅 Prashna 裁决摘要与模型协议中的术语密度、白话结论和稳定分段边界。
- [x] 将裁决摘要改为先给通俗结论、行动重点和观察时间，再把技术证据收至可展开的简洁部分。
- [x] 让流式文本按“结论—理由—条件—下一步”稳定段落续写，并补充白话可读性与连续渲染回归测试。
- [x] 保存包含 Prashna 白话裁决摘要与稳定分段流式输出的新 checkpoint，并确认公开站点已更新：`24805cc7`。
- [x] 审阅右上角工具区、分析发起函数与各模块的普通/流式报告调用边界。
- [x] 在右上角加入默认流式的图标模式开关，支持切换一次性输出并清晰显示当前模式。
- [x] 让所有模块共享结论优先、白话解释、证据可核对和限制后置的报告协议；增强流式稳态与完成一致性。
- [x] 增加模式切换、普通/流式等价结果和全模块报告策略回归测试，并完成构建和页面验证。
- [x] 保存包含输出模式开关、全模块报告协议与流式稳定性改进的新 checkpoint，并确认公开站点已更新：`c3652fa7`。
- [x] 审阅输出模式弹框的边框、阴影、图标、文案密度与顶部工具区对齐关系。
- [x] 重构弹框为简约纸本菜单，强化当前模式、留白与可访问焦点，减少不必要装饰。
- [x] 核对约 375px 视口的顶部工具区，并以样式契约测试覆盖菜单固定定位、安全区留白与箭头隐藏规则。
- [x] 在浏览器中实际聚焦 `menuitemradio` 并确认菜单保持展开；完成桌面模式切换、移动端样式契约与构建回归。
- [x] 保存包含弹框优化的 checkpoint，确认自动发布并记录版本号：`b00afc0e`。
- [x] 追踪一次性输出模式从前端状态、报告请求到终态渲染的路径，定位仍走流式视图的原因。
- [x] 修复一次性输出，使其仅在完整报告返回后进入 Markdown 终态视图，不创建或保留流式 DOM。
- [x] 提升全模块 AI 报告的具体结论、盘面证据、可执行下一步与白话表达，并避免无依据的泛化断言。
- [x] 增加一次性/流式渲染分支与报告质量协议的回归测试，并完成页面验证与生产构建。
- [x] 保存一次性输出修复与报告质量协议的 checkpoint，确认自动发布并记录版本号：`2501caf4`。
- [x] 审阅流式与一次性输出的等待、生成、完成与失败状态，明确用户仅看到阅读进度、结论正文与必要错误说明；一次性页面实测不显示草稿或内部过程。
- [x] 重构流式与一次性输出的会话反馈和结果容器，使两种模式分别清晰表达“逐段送达”与“完成后呈现”。
- [x] 过滤模型可能夹带的思考、推理或过程标签，仅保留结论、依据、条件、行动与必要限制。
- [x] 补充模式状态与可见内容过滤回归测试，完成一次性输出页面验证、双模式响应式样式契约核对和生产构建。
- [x] 保存双模式会话体验与过程过滤优化的 checkpoint，确认自动发布并记录版本号：`d68a47ec`。
- [x] 审阅流式报告结束后的原始 DOM 保留策略、排版容器、自动跟随与增量调度，定位卡顿与终态可读性问题：原始 DOM 未收束为稳定阅读版式，整页定位与终态扫描会放大长报告卡顿。
- [x] 将流式结束态整理为稳定、可阅读的轻量排版，不触发从头 Markdown 重渲染或内容闪烁。
- [x] 识别“现实里怎么验证”“接下来怎么做”等无 Markdown 标记的报告段落标题，补足终态阅读层级。
- [x] 降低高频增量追加、滚动和布局读取成本，并为长报告维持平滑的逐段送达体验。
- [x] 增加终态排版与高频分块性能回归测试，完成真实流式报告页面验证、类型检查与生产构建。
- [x] 在最终代码上再次完成真实流式长报告验证，复核章节层级、自动跟随与结束后的阅读排版：223 个增量、1101 字、无 restart、终态包含可识别阅读章节。
- [x] 在最终浏览器会话中发起真实流式长报告，并以端到端完成事件（223 增量、1101 字、无 restart）及终态排版契约确认无标记章节、自动跟随与卷轴收束逻辑生效。
- [x] 修复引用块后紧接 Markdown 或无标记章节标题时的终态分块边界，并增加 DOM 级回归断言。
- [x] 在最终浏览器会话中发起流式报告，并以终态 HTML 解析、样式契约和无 restart 端到端指标确认章节层级、活动光标与卷轴收束逻辑。
- [x] 使用自动化浏览器完成一份正式流式报告至终态：进入原始阅读容器，终态章节可见、活动光标为 `display:none`，卷轴收束样式生效。
- [x] 保存流式终态排版与性能优化的 checkpoint，确认自动发布并记录版本号：`09adf9ec`。
- [x] 归类控制台中的浏览器扩展、钱包注入、高德地图 SDK 与应用自身事件/定时器告警，明确修复边界：`contentscript`/`inpage`/Solana/Loki/Multiplex 来自扩展注入；`getImageData` 提示来自高德 SDK。
- [x] 审阅地图选点与流式会话的事件监听、`preventDefault` 和定时调度，修复项目自身可控的性能问题：顶部横向导航改用原生 `{ passive:false }` 监听，不再在 React 被动滚轮事件中调用 `preventDefault`。
- [x] 为原生非被动滚轮监听补充行为测试，并增加地图入口页面级回归断言。
- [x] 完成控制台复核、地图入口页面验证、单元测试、类型检查与生产构建。
- [ ] 保存控制台告警分层与原生滚轮修复的 checkpoint，确认自动发布并记录版本号。
